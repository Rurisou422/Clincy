{
  "name": "clincy-dma",
  "private": true,
  "version": "1.0.0",
  "description": "Premium DMA Firmware Provider Website",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "esbuild"
    ]
  }
}